/**
 * EcoSense AI — Vitest Setup File.
 *
 * Imports @testing-library/jest-dom to extend Vitest matchers
 * with DOM-specific assertions like toBeInTheDocument(),
 * toHaveTextContent(), toBeVisible(), etc.
 */

import "@testing-library/jest-dom";
